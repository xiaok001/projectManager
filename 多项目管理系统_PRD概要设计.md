# 多项目管理与风险提醒系统 —— PRD / 概要设计文档

版本：v1.0（MVP）
日期：2026-07-30
面向角色：部门经理（统揽视角），项目经理（数据录入方）

---

## 一、背景与目标

### 1.1 背景
公司现有项目管理系统面向项目经理的执行层需求（详细WBS、任务分解），无法满足部门经理"跨项目治理"的视角需求：每天需要快速掌握多个项目的风险、待处理问题和近期计划，作为介入沟通的依据。

### 1.2 目标
- **标准化**：统一立项、阶段、风险记录的字段与流程，使不同项目、不同项目经理填报的数据可横向比较。
- **自动化提醒**：系统自动判断阶段延期、风险停滞，无需人工巡检即可发现问题。
- **减少信息噪音**：部门经理无需查看WBS细节，只看聚合后的风险与计划。

### 1.3 非目标（本期不做）
- 不做详细任务/工时管理（仍由项目经理在原有WBS工具中管理）。
- 不做资源排期、人力分配。
- 不做客户满意度的自动采集（本期人工填写）。

---

## 二、用户角色与核心场景

| 角色 | 核心诉求 | 使用频率 |
|---|---|---|
| 部门经理 | 每天快速看到跨项目风险/问题与近期计划；收邮件摘要 | 每天 |
| 项目经理 | 按周更新阶段进度、登记/更新风险；部门经理也可协助更新 | 每周 |

### 核心场景
1. 部门经理打开系统首页，5分钟内了解所有在管项目（3-5个）的健康状况，决定今天要不要找哪个PM沟通。
2. 部门经理每天9:30收到一封邮件摘要，即使不打开系统也能获知重点。
3. 项目经理每周更新一次自己项目的阶段进度和风险状态。
4. 系统每周/每月自动生成结构化报告，供部门经理审阅后上报。

---

## 三、功能范围

### 3.1 MVP（本期）
1. 项目立项与基础信息管理
2. 阶段（里程碑）管理与自动逾期判定
3. 风险/问题登记与停滞判定（默认7天，可配置）
4. 部门经理首页 Dashboard（风险聚合区 / 未来计划区 / 项目健康度总览）
5. 项目健康评分（时间、风险、交付三项自动计算；客户满意度人工填写，不纳入自动加权，单独展示）
6. 每日邮件摘要（自然语言总结，非结构化列表）
7. AI 隐性风险探测（扫描PM填写的进展备注文本，识别潜在未登记风险并提示）
8. 周报/月报自动生成，含AI叙述性总结
9. 全局配置项（停滞阈值、邮件发送时间可调）
10. 操作留痕（每条更新记录操作人、更新时间）
11. 项目编号（人工录入）与风险编号（系统自动生成）体系
12. 每日邮件发送记录（发送内容、接收人、发送状态追溯）
13. 项目信息变更记录（人员/内容/范围变更登记，供项目复盘使用）

### 3.2 V2（下一版本规划，本期不实现）
- AI 历史风险处理经验匹配（基于历史相似风险给出处理建议）
- AI 智能优先级排序（综合项目等级、历史处理速度等做动态排序，替代当前的规则式排序）

---

## 四、数据模型设计

### 4.1 项目主表 Project
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| project_code | varchar | 项目编号，人工录入，无固定生成规则，系统仅做唯一性校验（重复提交时报错提示） |
| name | varchar | 项目名称 |
| type | varchar | 项目类型（默认：软件开发） |
| level | tinyint | 项目等级（如 P0/P1/P2），影响Dashboard排序 |
| amount | decimal | 项目金额 |
| pm_id | bigint | 项目经理（关联用户表） |
| start_date | date | 立项日期 |
| current_stage | varchar | 当前阶段 |
| status | varchar | 项目状态（进行中/已完成/已暂停） |
| satisfaction_score | tinyint | 客户满意度（人工填写，1-5或1-10，周期性更新） |
| created_at / updated_at | datetime | |

### 4.2 阶段表 ProjectStage
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| project_id | bigint | 关联项目 |
| stage_name | varchar | 启动/调研/开发/测试验收/上线/试运行/运维 |
| plan_start / plan_end | date | 计划起止 |
| actual_start / actual_end | date | 实际起止（未开始/未结束为空） |
| status | varchar | 未开始/进行中/已完成/已延期（延期由系统自动推导：actual_end为空且plan_end已过） |
| remark | text | 项目经理填写的自由文本备注（AI隐性风险探测的数据源） |
| updated_by / updated_at | | 操作留痕 |

### 4.3 风险/问题表 ProjectRisk
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| risk_code | varchar | 风险编号，系统自动生成，对外展示。生成规则：项目编号（人工录入的project_code）+ 年月日（如 项目编号为PRJ-A01，则生成 PRJ-A01-20260730），当日同项目多条时追加序号后缀 |
| project_id | bigint | 关联项目 |
| description | text | 风险/问题描述 |
| type | varchar | 风险（未发生）/ 问题（已发生） |
| severity | varchar | 高/中/低 |
| owner_id | bigint | 责任人 |
| status | varchar | 待处理/处理中/已解决/已关闭 |
| action_plan | text | 处理措施 |
| is_stale | boolean | 是否停滞（系统自动判断：last_updated超过阈值天数未变） |
| stale_override | boolean | 手动调整停滞状态的标记（允许人工纠正误判） |
| last_updated_at | datetime | 最近更新时间，停滞判定的依据 |
| updated_by | bigint | 最近操作人 |
| created_at | datetime | |

### 4.4 全局配置表 SystemConfig
| 字段 | 说明 |
|---|---|
| stale_threshold_days | 风险停滞判定天数，默认7 |
| daily_digest_send_time | 每日摘要发送时间，默认09:30 |
| digest_recipient_emails | 收件人邮箱列表 |

### 4.5 AI探测记录表 AiRiskSuggestion（隐性风险探测结果）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| project_id / stage_id | 关联来源 |
| source_text | 触发探测的原始备注文本 |
| suggested_risk_desc | AI生成的建议风险描述 |
| status | 待确认/已采纳登记为正式风险/已忽略 |
| created_at | |

### 4.6 每日邮件发送记录表 EmailDigestLog
用于追溯每次自动发送的实际情况，排查"是否发出去了""发的内容是什么"。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| send_date | date | 发送日期 |
| content | text | 实际发送的邮件正文内容（含AI生成的总结文本） |
| recipients | varchar | 接收人邮箱列表 |
| send_status | varchar | 成功/失败 |
| fail_reason | varchar | 失败原因（发送失败时记录） |
| sent_at | datetime | 实际发送时间 |

### 4.7 项目信息变更记录表 ProjectChangeLog
记录立项后发生的关键变更（人员变更、项目内容/范围变更等），作为项目结束复盘的原始资料，不同于阶段/风险的日常更新，聚焦"重大变化"。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| project_id | bigint | 关联项目 |
| change_type | varchar | 变更类型（人员变更/内容变更/范围变更/其他） |
| change_desc | text | 变更内容描述（如："PM由张三变更为李四，原因：XX" "需求范围新增XX模块"） |
| before_value | text | 变更前（可选，便于对比） |
| after_value | text | 变更后（可选） |
| changed_by | bigint | 记录人 |
| changed_at | datetime | 变更发生/记录时间 |

**联动提醒设计**：项目经理提交阶段更新（ProjectStage的remark或状态变更）时，系统在保存后弹出一个轻量提示——"本次更新是否涉及人员或项目内容变更？"，可选择"是"直接跳转到变更记录的快捷填写表单，也可选择"否"忽略。目的是降低变更记录表被漏填的概率，但不强制，避免打断项目经理的正常更新流程。

---

## 五、核心功能设计

### 5.1 Dashboard（部门经理首页）
三个区块，全量展示3-5个项目，不分页：

1. **风险/问题聚合区**：跨项目，排序规则 = 项目等级 → 严重程度 → 停滞天数（降序）。停滞（>7天未更新）标红。
2. **未来7-14天关键节点**：汇总所有项目未来两周内的计划阶段节点，以及已逾期未完成的阶段（单独置顶）。
3. **项目健康度总览**：每个项目一行，展示健康评分（红/黄/绿）、当前阶段、是否有高危/停滞风险的图标提示。点击进入项目详情（阶段时间线 + 风险列表 + 客户满意度）。

### 5.2 项目健康评分
- **时间维度**：基于阶段计划vs实际的偏差率自动计算。
- **风险维度**：基于高危风险数量 × 权重 + 平均停滞天数自动计算。
- **交付维度**：基于里程碑达成情况（是否按计划完成验收/上线）自动计算。
- **客户满意度**：由项目经理**按月**填写，不参与时间/风险/交付三项的自动加权，作为独立指标并列展示——避免用主观打分污染自动评分的可信度。该指标权重设置较低，主要用途是作为**项目经理个人考核**的参考依据之一，而非项目健康度的核心判断因子。
- 三项自动维度加权得出总分（1-100），映射为红/黄/绿。

### 5.3 每日邮件摘要
- 定时任务在配置的时间点（默认9:30）触发。
- 内容 = 聚合当日Dashboard三块数据 → 调用大模型生成一段自然语言总结（非结构化列表），突出"最需要关注的1-3件事"。
- 示例效果："XX项目测试阶段进度滞后2天，且关联的一个高危风险已停滞5天未更新，建议优先关注；YY项目本周将进入试运行阶段，请留意验收结果。"

### 5.4 AI 隐性风险探测
- 数据源：项目经理在阶段更新时填写的 `remark` 自由文本。
- 流程：项目经理保存阶段更新 → 后台异步调用大模型分析文本 → 若识别出潜在风险描述（如"进度比预期慢""客户有些不满意但还好"等表述）→ 写入 `AiRiskSuggestion` 表，状态为"待确认"。
- 呈现：Dashboard或项目详情页有一个"待确认的AI风险建议"入口，项目经理或部门经理可以一键"采纳并登记为正式风险"或"忽略"。
- 设计原则：AI只做**建议**，不自动写入正式风险表，避免误报污染风险台账。

### 5.5 周报/月报自动生成
- 数据来源：直接从阶段表、风险表的结构化数据统计（无需额外录入）。
- 周报内容：本周完成的阶段节点、新增/关闭的风险、下周计划节点。
- 月报内容：在周报基础上增加"本月延期项目及原因"、"风险平均处理时长"等统计。
- AI叙述生成：将统计数据转成一段可直接用于向上汇报的文字总结，部门经理审阅/微调后使用。

---

## 六、非功能需求

- 数据量小（3-5个项目），无需考虑高并发和分库分表，优先保证开发效率和后续可维护性。
- 操作留痕：所有阶段/风险的变更需记录操作人和时间，用于追溯。
- 全局配置项需可在界面上调整，不能写死在代码中。

---

## 七、技术选型建议

### 7.1 后端语言：推荐 Java
理由：
- 你的团队本身已有后端Java开发能力（部门内技术团队即以Java为主），复用现有人力和代码规范成本最低。
- 与前端Vue3的组合（Spring Boot + Vue3）是当前最主流、生态最成熟的组合之一，第三方库（定时任务、邮件发送、MySQL集成）都很完善。
- 相比Python，Java在长期维护、类型安全、团队协作规范性上更适合"内部管理系统"这类要长期跑、多人维护的场景；Python更适合数据分析/脚本类场景，本系统偏业务系统属性，Java更合适。

若团队后续想快速验证MVP、追求极致开发速度，Python（如FastAPI）也是可选项，但考虑到你团队现有Java技术栈的复用度，仍建议Java。

### 7.2 技术栈组合
| 层 | 选型 |
|---|---|
| 前端 | Vue3 + TypeScript（可选）+ Element Plus |
| 后端 | Java + Spring Boot 3 |
| 数据库 | MySQL |
| 用户体系 | 自建账号体系（独立登录/权限管理，不依赖公司现有SSO） |
| 定时任务 | Spring Scheduler（阈值判定、每日摘要触发） |
| 邮件发送 | Spring Mail（SMTP），发送结果写入 EmailDigestLog |
| AI能力集成 | 需同时支持 **DeepSeek API**（云端，默认）和 **Ollama API**（本地部署）两种调用方式。默认调用DeepSeek API处理AI能力（每日摘要生成、隐性风险探测、报告叙述生成）；Ollama优先考虑部署在本地环境，作为备选/离线场景使用。后端建议做一层统一的AI服务抽象接口（如定义 `AiProvider` 接口），DeepSeek和Ollama各自实现，通过配置切换。异步任务处理，避免阻塞主流程 |

---

## 八、里程碑规划

| 阶段 | 内容 |
|---|---|
| Sprint 1 | 项目/阶段/风险基础数据模型 + 立项、阶段更新、风险登记的CRUD界面 |
| Sprint 2 | Dashboard三区块（风险聚合、未来节点、健康度总览）+ 全局配置 |
| Sprint 3 | 项目健康评分计算 + 每日邮件摘要（先做结构化版本） |
| Sprint 4 | AI能力接入：每日摘要自然语言化、隐性风险探测、周报月报叙述生成 |
| Sprint 5 | 周报/月报完整功能 + 联调、验收上线 |

（以上Sprint节奏仅为建议，具体周期可根据实际开发资源调整）

---

## 九、待确认的开放问题

1. Ollama本地部署的具体环境细节（本地机器配置、是否常驻运行以保证定时任务能调用到）需要在详细设计阶段确认，避免出现"本地机器关机导致AI功能不可用"的情况——建议评估是否需要为Ollama准备一台常驻运行的机器/服务器，而非个人办公电脑。
2. 阶段更新与变更记录联动提醒的具体触发时机（是每次阶段更新都问，还是仅当状态变化较大时才问）可以在详细设计阶段结合实际使用体验再调整，避免提示过于频繁造成打扰。

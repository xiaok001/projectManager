# 多项目管理与风险提醒系统 —— PRD / 概要设计文档

版本：v2.0（已实现）
日期：2026-08-04
面向角色：部门经理（统揽视角），项目经理（数据录入方）

---

## 一、背景与目标

### 1.1 背景
公司现有项目管理系统面向项目经理的执行层需求（详细WBS、任务分解），无法满足部门经理"跨项目治理"的视角需求：每天需要快速掌握多个项目的风险、待处理问题和近期计划，作为介入沟通的依据。

### 1.2 目标
- **标准化**：统一立项、阶段、风险、待办记录的字段与流程，使不同项目、不同项目经理填报的数据可横向比较。
- **自动化提醒**：系统自动判断阶段延期、风险停滞、待办逾期，无需人工巡检即可发现问题。
- **AI赋能**：通过AI自动识别隐性风险、生成周报摘要、辅助决策。
- **减少信息噪音**：部门经理无需查看WBS细节，只看聚合后的风险与计划。

### 1.3 非目标（本期不做）
- 不做详细任务/工时管理（仍由项目经理在原有WBS工具中管理）。
- 不做资源排期、人力分配。
- 不做客户满意度的自动采集（本期人工填写）。

---

## 二、用户角色与核心场景

### 2.1 角色体系

| 角色 | 标识 | 核心诉求 | 权限范围 |
|---|---|---|---|
| 超级管理员 | admin | 全局管理，系统配置 | 所有功能 + 系统管理 |
| 项目经理 | pm | 按周更新阶段进度、登记风险/待办 | 仅自己负责的项目 |
| 查看者 | viewer | 了解项目进展 | 只读权限 |

### 2.2 数据权限
- 角色支持「全部项目」或「指定项目」两种数据权限模式
- 部门经理默认全部项目，项目经理默认仅自己负责的项目
- 后端所有查询接口在服务层强制过滤，不依赖前端隐藏

### 2.3 核心场景
1. 部门经理打开系统首页，5分钟内了解所有在管项目的健康状况，决定今天要不要找哪个PM沟通。
2. 部门经理每天9:30收到一封邮件摘要，即使不打开系统也能获知重点。
3. 项目经理每周更新自己项目的阶段进度、风险状态和待办事项。
4. 系统每周自动生成AI叙述性周报，供部门经理审阅后上报。
5. 系统每晚自动扫描项目阶段备注，AI识别潜在未登记风险。
6. 项目经理更新阶段备注后，AI异步分析文本并提示潜在风险。
7. 逾期待办自动转为风险记录，确保问题不被遗漏。

---

## 三、功能范围

### 3.1 已实现功能

| 序号 | 功能 | 说明 |
|---|---|---|
| 1 | 项目立项与管理 | 项目编号唯一性校验，创建后自动初始化7个标准阶段，预期结束日期，WBS在线/离线文档 |
| 2 | 阶段管理 | 预估/实际人天、预估/实际成本、完成进度(0-100%)，进度与状态联动，延期自动判定 |
| 3 | 风险/问题管理 | 风险编号自动生成，8种类型枚举+自定义，停滞判定+手动覆盖 |
| 4 | 项目待办 | 待办编号自动生成，阶段/来源/优先级/紧急程度/负责人/进度/阻塞问题/风险说明，逾期自动转风险 |
| 5 | Dashboard | 风险聚合区(可点击跳转处理) + 未来关键节点(含数据来源提示) + 项目健康度总览 |
| 6 | 健康评分 | 时间35% + 风险40% + 交付25%自动计算，客户满意度独立展示，所有权重可配置 |
| 7 | AI风险探测 | 阶段备注异步分析 + 每晚22:00全量扫描 + 手动扫描(含原理说明) |
| 8 | AI周报 | 聚合本周阶段/风险/待办数据，AI生成叙述性总结，排除运维阶段和已结束项目 |
| 9 | 每日邮件摘要 | 定时发送(默认9:30)，含AI自然语言总结，发送记录可追溯，支持测试发送 |
| 10 | 用户管理 | CRUD + 邮箱/手机号格式校验 + 角色关联 |
| 11 | 角色管理 | CRUD + 页面/按钮权限树分配 + 数据权限(项目级) |
| 12 | 系统配置 | 健康权重/停滞阈值/邮件时间/AI配置，全部可在界面调整 |
| 13 | 操作日志 | AOP自动记录增删改(GET不记录)，按模块/类型过滤 |
| 14 | 定时任务管理 | 任务列表/启用禁用/手动执行/执行记录 |
| 15 | 项目变更记录 | 人员/内容/范围/风险变更登记，支持变更前后对比展示 |
| 16 | 认证与安全 | JWT无状态认证，路由守卫+接口鉴权，403自动跳登录，忘记密码(验证账号→验证邮箱→发送新密码) |
| 17 | WBS文档管理 | 在线链接(可点击跳转) + 离线附件(拖拽上传/下载/覆盖替换) |

### 3.2 V2（下一版本规划）
- AI 历史风险处理经验匹配（基于历史相似风险给出处理建议）
- AI 智能优先级排序（综合项目等级、历史处理速度等做动态排序）
- 月报自动生成

---

## 四、数据模型设计

### 4.1 用户与权限（4张表）

**sys_user** - 用户表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| username | varchar | 用户名，唯一 |
| password | varchar | 密码(BCrypt加密) |
| real_name | varchar | 真实姓名 |
| email | varchar | 邮箱 |
| phone | varchar | 手机号 |
| role | varchar | 角色标识(DEPT_MANAGER/PM)，向后兼容 |
| role_id | bigint | 关联sys_role |
| status | tinyint | 状态(1启用/0禁用) |

**sys_role** - 角色表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| role_name | varchar | 角色名称 |
| role_key | varchar | 角色标识，唯一 |
| data_scope | varchar | 数据权限(all=全部/custom=指定项目) |
| status | tinyint | 状态 |

**sys_permission** - 权限表（树形结构）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| parent_id | bigint | 父权限ID(0=顶级) |
| perm_name | varchar | 权限名称 |
| perm_key | varchar | 权限标识 |
| type | varchar | menu(页面)/button(按钮) |
| path | varchar | 页面路由(type=menu时) |

**sys_role_permission** / **sys_role_project** - 关联表

### 4.2 项目主表 Project
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| project_code | varchar | 项目编号，人工录入，唯一性校验 |
| name | varchar | 项目名称 |
| type | varchar | 项目类型（默认：软件开发） |
| level | tinyint | 项目等级(0-P0/1-P1/2-P2) |
| amount | decimal | 项目金额 |
| pm_id | bigint | 项目经理 |
| start_date | date | 立项日期 |
| expected_end_date | date | 预期结束日期 |
| wbs_online_url | varchar | WBS在线文档链接 |
| wbs_offline_file | varchar | WBS离线附件路径 |
| current_stage | varchar | 当前阶段 |
| status | varchar | 进行中/已完成/已暂停 |
| satisfaction_score | tinyint | 客户满意度(1-10) |

### 4.3 阶段表 ProjectStage
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| project_id | bigint | 关联项目 |
| stage_name | varchar | 阶段名(7个标准阶段) |
| sort_order | int | 阶段排序 |
| plan_start / plan_end | date | 计划起止 |
| actual_start / actual_end | date | 实际起止 |
| status | varchar | 未开始/进行中/已完成/已延期 |
| remark | text | 备注(AI风险探测数据源) |
| plan_man_days / actual_man_days | decimal | 预估/实际人天 |
| plan_cost / actual_cost | decimal | 预估/实际成本 |
| progress | int | 完成进度(0-100)，联动状态 |

### 4.4 风险/问题表 ProjectRisk
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| risk_code | varchar | 风险编号(项目编号-日期-序号) |
| project_id | bigint | 关联项目 |
| description | text | 风险/问题描述 |
| type | varchar | 风险/问题 |
| severity | varchar | 高/中/低 |
| owner_id | bigint | 责任人 |
| status | varchar | 待处理/处理中/已解决/已关闭 |
| action_plan | text | 处理措施 |
| is_stale | boolean | 是否停滞 |
| stale_override | boolean | 手动覆盖停滞状态 |
| last_updated_at | datetime | 停滞判定依据 |

### 4.5 待办事项表 ProjectTodo
| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| todo_code | varchar | 待办编号(项目编号-TD-日期-序号) |
| project_id | bigint | 所属项目 |
| stage_id | bigint | 所属阶段 |
| title | varchar | 待办事项 |
| source | varchar | 来源(会议纪要/客户需求/内部评估/风险跟踪/其他) |
| priority | varchar | 优先级(高/中/低) |
| urgency | varchar | 紧急程度(紧急/特急/普通) |
| owner_id | bigint | 负责人ID |
| owner_name | varchar | 负责人姓名(支持手动输入) |
| plan_start / plan_end | date | 计划起止 |
| actual_end | date | 实际完成时间 |
| status | varchar | 待处理/进行中/已完成/已取消/已逾期 |
| progress | int | 完成百分比(0-100) |
| block_issue | text | 阻塞问题 |
| risk_desc | text | 风险说明 |
| output_desc | varchar | 输出物 |

### 4.6 其他表
| 表名 | 说明 |
|---|---|
| system_config | 全局配置(18项可配参数) |
| ai_risk_suggestion | AI风险建议(待确认/已采纳/已忽略) |
| email_digest_log | 邮件发送记录 |
| project_change_log | 项目变更记录(含change_field变更字段名，支持前后对比) |
| operation_log | 操作日志(AOP自动记录) |
| scheduled_task | 定时任务配置(4个任务) |
| scheduled_task_log | 定时任务执行记录 |

---

## 五、核心功能设计

### 5.1 Dashboard（部门经理首页）
三个区块，全量展示项目：

1. **风险/问题聚合区**：跨项目，排序 = 项目等级 → 严重程度 → 停滞天数。停滞标红。每行可点击跳转项目详情风险Tab，有「去处理」按钮。
2. **未来7-14天关键节点**：汇总所有项目未来两周内的计划阶段节点 + 已逾期未完成阶段（置顶）。底部提示数据来源。
3. **项目健康度总览**：卡片式展示每个项目的健康评分、当前阶段、高危/停滞风险数量。点击卡片进入项目详情。

### 5.2 项目健康评分
```
健康总分 = 时间维度 × 35% + 风险维度 × 40% + 交付维度 × 25%
```
- **时间维度**：100 - 延期天数 × 每天扣分(默认2)
- **风险维度**：100 - 高危×15 - 中危×8 - 低危×3 - 停滞×10
- **交付维度**：按时完成阶段数 ÷ 应完成阶段数 × 100
- **客户满意度**：独立展示(1-10)，不参与加权，项目经理个人考核参考
- 颜色映射：≥80绿 / 60-79黄 / <60红，阈值可配置

### 5.3 阶段管理
- 7个标准阶段：启动→调研→开发→测试验收→上线→试运行→运维
- 阶段名可编辑，支持下拉选择或自定义输入
- 进度与状态联动：0%=未开始，1%-99%=进行中，100%=已完成
- 预估/实际人天、预估/实际成本、完成进度
- 延期自动判定：定时任务每小时刷新，plan_end已过且actual_end为空→自动标记已延期

### 5.4 风险管理
- 风险编号自动生成：`{项目编号}-{日期}-{序号}`
- 8种类型枚举(技术/进度/需求/资源/质量/外部/成本/管理风险)+ 支持自定义
- 停滞自动判定：超过配置天数(默认7天)未更新→标红，支持手动覆盖
- 项目结束校验：选择「已完成」时检查非运维阶段是否全部完成

### 5.5 项目待办
- 待办编号自动生成：`{项目编号}-TD-{日期}-{序号}`
- 完整字段：阶段/来源/优先级/紧急程度/负责人(支持手动输入)/进度/阻塞问题/风险说明/输出物
- 进度联动状态
- 逾期待办自动创建风险记录（定时任务每小时检查）
- 全局待办页面 + 项目详情待办Tab

### 5.6 AI能力
- **隐性风险探测**：阶段备注保存后异步调用AI分析 → 写入建议表(待确认)
- **全量扫描**：每晚22:00自动执行，遍历所有进行中项目的阶段备注
- **手动扫描**：前端按钮触发，弹窗说明扫描原理后执行
- **周报AI总结**：聚合数据发送给AI，生成3-5句叙述性总结
- **AI提供者**：支持Ollama(本地)/DeepSeek(云端)切换，统一AiProvider接口

### 5.7 周报生成
- 报告周期：本周一至周日
- 数据来源：本周完成阶段(排除运维)、新增/关闭风险、风险统计、下周计划节点、待办统计
- 项目范围：全部项目(自动排除已结束)或单个项目(已结束时提示确认)
- AI叙述性总结 + 结构化数据分区展示

### 5.8 定时任务
| 任务 | 频率 | 说明 |
|---|---|---|
| 阶段延期刷新 | 每小时 | 自动标记已延期阶段 |
| 风险停滞刷新 | 每小时:30 | 自动标记停滞风险 |
| 待办逾期刷新 | 每小时:15 | 逾期待办自动创建风险 |
| AI风险扫描 | 每晚22:00 | 全量扫描阶段备注 |

- 所有任务支持启用/禁用、手动执行
- 执行后自动记录日志（触发方式/状态/结果/耗时）
- 执行前检查任务是否启用，禁用则跳过

### 5.9 每日邮件摘要
- 定时任务在配置的时间点（默认9:30）触发
- 聚合Dashboard三块数据 → 调用AI生成自然语言总结
- AI失败时降级为结构化列表，邮件照常发送
- 支持手动触发和测试发送

### 5.10 认证与权限
- JWT无状态认证，24小时过期
- 忘记密码：验证账号→验证邮箱→生成随机密码→发送邮件
- 路由守卫：未登录跳登录页，403跳登录页，未匹配路由跳404
- 接口鉴权：Spring Security + 自定义JWT过滤器

### 5.11 WBS文档管理
- **在线链接**：展示态可点击跳转新窗口，编辑态输入框+保存/取消
- **离线附件**：拖拽上传(支持.doc/.xls/.pdf/.mpp/.zip等)，上传成功后显示文件名+下载+重新上传
- 文件存储路径：`project-files/{项目编号}/{日期}/{原始文件名}`
- 覆盖式替换，数据库只保留最新路径

---

## 六、非功能需求

- 数据量小（3-5个项目），无需考虑高并发和分库分表，优先保证开发效率和后续可维护性。
- 操作留痕：所有阶段/风险/待办的变更需记录操作人和时间。
- 全局配置项需可在界面上调整，不能写死在代码中。
- 邮箱/手机号格式校验：前端实时校验 + 后端提交时二次校验，非必填。

---

## 七、技术选型

| 层 | 选型 |
|---|---|
| 前端 | Vue3 + TypeScript + Element Plus + Vite |
| 后端 | Java 17 + Spring Boot 3.2 + MyBatis-Plus 3.5 |
| 数据库 | MySQL 8.0+ |
| 认证 | Spring Security + JWT |
| 定时任务 | Spring Scheduler |
| 邮件 | Spring Mail（163 SMTP） |
| AI能力 | Ollama(本地) / DeepSeek(云端)，AiProvider接口抽象，配置切换 |
| 操作日志 | Spring AOP（仅记录增删改） |
| 文件存储 | 本地磁盘（WBS附件） |

---

## 八、侧边栏导航结构

```
首页概览
项目管理
AI风险建议
项目待办
运营中心
  ├─ 项目报告（AI周报）
  └─ 定时任务
系统管理（仅部门经理）
  ├─ 系统配置
  ├─ 用户管理
  ├─ 角色管理
  └─ 操作日志
```

---

## 九、部署信息

| 项目 | 信息 |
|---|---|
| 服务器 | 192.168.4.161 |
| 部署目录 | /opt/nyk/pm |
| 前端地址 | http://192.168.4.161:8090 |
| 后端地址 | http://192.168.4.161:8080 |
| Ollama | http://192.168.4.161:11434 (qwen2.5:7b) |
| 数据库 | 192.168.4.195:3306/project_manager |
| JDK | 17 ( /opt/jdk17 ) |
| Nginx | 端口8090, 配置 /etc/nginx/conf.d/pm.conf |
| 启动脚本 | /opt/nyk/pm/start.sh |

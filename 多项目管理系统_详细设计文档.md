# 多项目管理与风险提醒系统 —— 详细设计文档

版本：v2.0（配套PRD v2.0，已实现）
说明：本文档聚焦"怎么做"，涵盖接口设计、计算公式、验收标准。所有数值类参数均为可配置项，以下为默认值。

---

## 一、角色与权限

### 1.1 角色定义
| 角色 | 标识 | 说明 |
|---|---|---|
| 超级管理员 | admin | 拥有全部权限，管理系统配置、用户、角色、定时任务 |
| 项目经理 | pm | 操作自己负责的项目，数据自动过滤 |
| 查看者 | viewer | 只读权限 |

### 1.2 权限体系
- **页面权限**：菜单级，控制侧边栏和路由访问
- **按钮权限**：操作级，控制页面内的增删改按钮
- **数据权限**：项目级，角色可配置「全部项目」或「指定项目」
- 权限通过 `sys_permission` 表管理（树形结构，type=menu/button）
- 角色通过 `sys_role_permission` 关联权限，通过 `sys_role_project` 关联数据权限

### 1.3 权限矩阵

| 资源/操作 | 超级管理员 | 项目经理(本人) | 项目经理(他人) | 查看者 |
|---|---|---|---|---|
| 项目 - CRUD | ✅ | ✅ | ❌ | 只读 |
| 阶段 - 更新 | ✅ | ✅ | ❌ | ❌ |
| 风险 - CRUD | ✅ | ✅ | ❌ | ❌ |
| 待办 - CRUD | ✅ | ✅ | ❌ | ❌ |
| AI建议 - 采纳/忽略 | ✅ | ✅ | ❌ | ❌ |
| 变更记录 | ✅ | ✅ | ❌ | ❌ |
| Dashboard | ✅(全部) | ✅(本人) | - | ✅(本人) |
| 周报 | ✅(全部) | ✅(本人) | ❌ | ✅(本人) |
| 系统配置 | ✅ | ❌ | ❌ | ❌ |
| 用户/角色管理 | ✅ | ❌ | ❌ | ❌ |
| 操作日志 | ✅ | ❌ | ❌ | ❌ |
| 定时任务 | ✅ | ❌ | ❌ | ❌ |
| 邮件记录 | ✅ | ❌ | ❌ | ❌ |

**后端实现**：所有查询接口在服务层强制过滤 `project_id in (当前用户数据权限范围)`，部门经理豁免。非本人项目返回403，不返回空数据。

---

## 二、项目健康评分

### 2.1 总分构成
```
健康总分 = 时间维度 × 35% + 风险维度 × 40% + 交付维度 × 25%
```
权重存于 `system_config`：`health_weight_time` / `health_weight_risk` / `health_weight_delivery`，三者之和必须为100%，配置页保存时校验。

### 2.2 时间维度（time_score）
```
time_score = max(0, 100 - Σ(每个已到期阶段的延期天数 × 每天扣分))
```
- 延期天数：已完成阶段 = max(0, actual_end - plan_end)；未完成阶段 = max(0, 今天 - plan_end)
- 每天扣分默认2分，配置 `time_delay_penalty_per_day`

### 2.3 风险维度（risk_score）
```
risk_score = max(0, 100 - 高危×15 - 中危×8 - 低危×3 - 停滞×10)
```
- 仅统计待处理/处理中的风险
- 配置：`risk_penalty_high` / `risk_penalty_medium` / `risk_penalty_low` / `risk_penalty_stale`

### 2.4 交付维度（delivery_score）
```
delivery_score = (按时完成阶段数 ÷ 已到应完成时间阶段总数) × 100
```
- 按时完成：actual_end ≤ plan_end
- 分母为0时默认100分

### 2.5 颜色映射
| 总分区间 | 颜色 | 配置键 |
|---|---|---|
| ≥80 | 绿 | `health_score_green_min` |
| 60-79 | 黄 | `health_score_yellow_min` |
| <60 | 红 | — |

### 2.6 客户满意度
- 独立展示，1-10分制，月度更新
- 不参与健康总分计算
- 用途：项目经理个人考核参考

---

## 三、进度与状态联动

| 进度值 | 自动设置状态 | 额外动作 |
|---|---|---|
| 0% | 未开始 | — |
| 1%-99% | 进行中 | 自动填写实际开始日期 |
| 100% | 已完成 | 自动填写实际结束日期 |

- 前端拖动滑块实时联动状态字段
- 后端保存时再次校验确保一致性
- 进度 < 100 时清除 actual_end（允许回退进度）

---

## 四、项目结束校验

编辑项目状态改为「已完成」时：
1. 检查所有非运维阶段是否已完成
2. 如有未完成阶段 → 前端显示警告条（列出未完成阶段名称和状态）
3. 点击保存时二次确认弹窗
4. 后端兜底校验，未完成时拒绝保存

---

## 五、编号生成规则

### 5.1 风险编号
格式：`{项目编号}-{yyyyMMdd}-{序号}`
示例：`PRJ-A01-20260803-02`
- 同项目同日多条时自动追加序号

### 5.2 待办编号
格式：`{项目编号}-TD-{yyyyMMdd}-{序号}`
示例：`PRJ-A01-TD-20260803-01`

---

## 六、API 接口清单

统一前缀：`/api/v1`，返回：`{code, message, data}`

### 6.1 认证 Auth
| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| POST | /auth/login | 登录，返回JWT | 公开 |
| POST | /auth/verify-username | 验证账号是否存在 | 公开 |
| POST | /auth/reset-password | 验证账号+邮箱，发送新密码 | 公开 |
| GET | /auth/users | 用户列表 | 已认证 |

### 6.2 用户管理 User
| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| GET | /users | 用户列表 | 已认证 |
| GET | /users/{id} | 用户详情 | 已认证 |
| POST | /users | 创建用户 | DEPT_MANAGER |
| PUT | /users/{id} | 编辑用户 | DEPT_MANAGER |
| DELETE | /users/{id} | 删除用户(物理删除，admin不可删) | DEPT_MANAGER |

### 6.3 角色管理 Role
| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| GET | /roles | 角色列表 | DEPT_MANAGER |
| POST | /roles | 创建角色 | DEPT_MANAGER |
| PUT | /roles/{id} | 编辑角色 | DEPT_MANAGER |
| DELETE | /roles/{id} | 删除角色(有用户关联时拒绝) | DEPT_MANAGER |
| GET | /roles/{id}/permissions | 获取角色权限ID列表 | DEPT_MANAGER |
| PUT | /roles/{id}/permissions | 分配权限 | DEPT_MANAGER |
| GET | /roles/{id}/data-scope | 获取数据权限 | DEPT_MANAGER |
| PUT | /roles/{id}/data-scope | 分配数据权限(all/custom+项目列表) | DEPT_MANAGER |

### 6.4 权限 Permission
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /permissions/tree | 完整权限树 |
| GET | /permissions/tree/{roleId} | 带勾选状态的权限树 |

### 6.5 项目 Project
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /projects | 项目列表（支持name/projectCode/level查询） |
| POST | /projects | 创建项目（含唯一性校验+自动初始化阶段） |
| GET | /projects/{id} | 项目详情 |
| PUT | /projects/{id} | 编辑项目（含状态变更校验） |
| DELETE | /projects/{id} | 删除项目（级联删除阶段/风险/待办/变更） |
| PUT | /projects/{id}/satisfaction | 更新客户满意度(1-10) |
| PUT | /projects/{id}/wbs-url | 更新WBS在线链接（允许清空） |
| POST | /projects/{id}/wbs-file | 上传WBS附件（覆盖旧文件） |
| GET | /projects/{id}/wbs-file | 下载WBS附件 |

### 6.6 阶段 ProjectStage
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /projects/{id}/stages | 阶段列表 |
| PUT | /stages/{id} | 更新阶段（进度联动状态，异步触发AI探测） |

### 6.7 风险 ProjectRisk
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /projects/{id}/risks | 项目风险列表 |
| POST | /projects/{id}/risks | 登记风险（编号自动生成） |
| GET | /risks/aggregated | 跨项目风险聚合（Dashboard用） |
| PUT | /risks/{id} | 更新风险 |
| PUT | /risks/{id}/stale-override | 手动覆盖停滞状态 |

### 6.8 待办 ProjectTodo
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /projects/{id}/todos | 项目待办列表 |
| POST | /projects/{id}/todos | 项目级创建待办 |
| GET | /todos/page | 全局待办分页（支持projectId/status/priority/keyword） |
| POST | /todos | 全局创建待办（需传projectId） |
| PUT | /todos/{id} | 编辑待办 |
| DELETE | /todos/{id} | 删除待办 |

### 6.9 Dashboard
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /dashboard/summary | 首页三区块聚合数据 |
| GET | /dashboard/health | 项目健康度列表 |
| GET | /dashboard/health/{projectId} | 单项目健康度 |

### 6.10 全局配置 SystemConfig
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /config | 全部配置列表 |
| PUT | /config | 批量更新配置（含权重之和校验） |

### 6.11 AI建议 AiRiskSuggestion
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /ai-suggestions | 建议列表 |
| GET | /ai-suggestions/page | 分页（支持projectCode/projectName/startDate/endDate/status） |
| POST | /ai-suggestions/{id}/accept | 采纳（自动创建正式风险） |
| POST | /ai-suggestions/{id}/ignore | 忽略 |
| POST | /ai-suggestions/scan | 手动全量AI扫描 |

### 6.12 邮件摘要 EmailDigest
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /digest/send-now | 手动触发摘要发送 |
| POST | /digest/test?email=xxx | 测试邮件发送 |
| GET | /digest/logs | 邮件发送记录分页 |

### 6.13 变更记录 ProjectChangeLog
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /projects/{id}/changes | 登记变更（含changeType/changeField/beforeValue/afterValue） |
| GET | /projects/{id}/changes | 变更历史（关联操作人姓名） |

### 6.14 周报 Report
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /reports/weekly?projectId= | AI周报（排除运维阶段和已结束项目） |

### 6.15 操作日志 OperationLog
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /operation-logs | 分页查询（支持module/operation过滤） |

### 6.16 定时任务 ScheduledTask
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /scheduled-tasks | 任务列表 |
| PUT | /scheduled-tasks/{id}/toggle | 启用/禁用 |
| POST | /scheduled-tasks/{id}/run | 手动执行 |
| GET | /scheduled-tasks/logs | 执行记录分页（支持taskId过滤） |

---

## 七、AI服务抽象接口

```
interface AiProvider {
    String chat(String prompt);
}
class DeepSeekProvider implements AiProvider { ... }
class OllamaProvider implements AiProvider { ... }
```
- 配置 `ai.provider = deepseek | ollama` 切换
- 四个AI功能统一通过 `AiProvider` 调用：每日摘要、隐性风险探测、全量扫描、周报生成
- 降级策略：AI失败时摘要/报告退化为纯结构化文本，记录失败日志

---

## 八、定时任务设计

### 8.1 任务清单
| 任务 | Cron | 说明 |
|---|---|---|
| refreshDelayedStages | `0 0 * * * ?` | 每小时刷新阶段延期状态 |
| refreshStaleRisks | `0 30 * * * ?` | 每小时刷新风险停滞状态 |
| refreshOverdueTodos | `0 15 * * * ?` | 每小时刷新待办逾期+自动创建风险 |
| nightlyAiRiskScan | `0 0 22 * * ?` | 每晚22:00 AI全量扫描 |

### 8.2 执行日志
- 每次执行自动记录：任务ID、触发方式(自动/手动)、状态(成功/失败)、结果、耗时
- 执行前检查 `scheduled_task.status`，禁用则跳过
- 手动执行通过 Controller 触发，走同样的日志记录

### 8.3 逾期待办自动转风险
```
检查条件: status != 已完成 AND status != 已取消 AND plan_end <= 今天
动作: 1. 更新状态为"已逾期"
      2. 创建正式风险记录(编号自动生成，类型=问题，严重程度=按优先级映射)
```

---

## 九、文件上传设计（WBS附件）

### 9.1 存储路径
```
{upload-root}/{项目编号}/{yyyyMMdd}/{原始文件名}
```
默认 `upload-root = ./project-files`，配置 `file.upload-root`

### 9.2 规则
- 覆盖式替换，数据库只保留最新相对路径
- 最大 50MB（配置 `server.servlet.multipart.max-file-size`）
- 支持格式：.doc .docx .xls .xlsx .pdf .mpp .zip .rar

---

## 十、验收标准

### AC-1 项目立项
- [x] 项目编号重复时返回明确错误提示
- [x] 创建后7个标准阶段自动初始化（状态"未开始"）
- [x] 预期结束日期、WBS链接/附件可填写

### AC-2 阶段管理
- [x] 进度0%→未开始，1%-99%→进行中，100%→已完成
- [x] 进度<100时清除actual_end（允许回退）
- [x] plan_end已过且actual_end为空→自动标记"已延期"
- [x] 阶段名可编辑，支持下拉选择或自定义

### AC-3 风险管理
- [x] risk_code后端自动生成，前端不可编辑
- [x] 同项目同日多条风险编号不重复
- [x] 停滞判定+手动覆盖
- [x] 风险类型下拉枚举+支持自定义

### AC-4 项目待办
- [x] 待办编号自动生成
- [x] 进度联动状态
- [x] 逾期待办自动创建风险
- [x] 负责人支持手动输入和下拉选择

### AC-5 Dashboard
- [x] 风险聚合区可点击跳转项目详情风险Tab
- [x] 项目健康度卡片可点击跳转项目详情
- [x] 未来节点含数据来源提示
- [x] 排序规则：项目等级→严重程度→停滞天数

### AC-6 健康评分
- [x] 三项权重之和必须100%，保存时校验
- [x] 客户满意度不影响健康总分

### AC-7 邮件
- [x] 自动发送+手动发送+测试发送
- [x] AI失败时降级为结构化列表
- [x] 发送记录可追溯

### AC-8 AI风险探测
- [x] 阶段备注保存后异步触发，不阻塞
- [x] 结果仅写入建议表（待确认）
- [x] 采纳后自动创建正式风险

### AC-9 周报
- [x] 排除运维阶段和已结束项目
- [x] 选择已结束项目时前端提示确认
- [x] AI生成叙述性总结

### AC-10 定时任务
- [x] 任务列表展示Cron/说明/状态
- [x] 启用/禁用切换
- [x] 手动执行+执行记录
- [x] 执行前检查启用状态

### AC-11 权限隔离
- [x] 非本人项目返回403
- [x] 角色数据权限过滤（后端服务层强制）
- [x] 403自动跳转登录页

### AC-12 项目结束校验
- [x] 选择「已完成」时检查非运维阶段
- [x] 未完成阶段显示警告+二次确认
- [x] 后端兜底校验

### AC-13 WBS文档
- [x] 在线链接可点击跳转
- [x] 离线附件拖拽上传/下载/覆盖替换
- [x] 空链接可清空，非空时校验URL格式

---

## 十一、备注

- 所有权重/系数/阈值均为可配置项，建议试运行1-2个项目周期后微调
- AI扫描去重逻辑：按 `(stageId + remark哈希)` 建立索引，相同内容不重复生成建议
- 操作日志仅记录 POST/PUT/DELETE，GET 查询不记录

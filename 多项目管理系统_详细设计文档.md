# 多项目管理与风险提醒系统 —— 详细设计文档

版本：v3.0（配套PRD v3.0）

---

## 一、角色与权限

### 1.1 权限体系
- **页面权限**：菜单级，控制侧边栏和路由访问
- **按钮权限**：操作级，控制页面内的增删改按钮
- **数据权限**：项目级，角色可配置「全部项目」或「指定项目」
- 前端动态加载：登录后调用 `GET /auth/permissions` 获取权限树，侧边栏根据权限动态渲染
- 内置角色（is_system=1）不可删除

### 1.2 权限矩阵

| 操作 | 超级管理员 | 项目经理(本人) | 查看者 |
|---|---|---|---|
| 项目CRUD | ✅ | ✅ | 只读 |
| 阶段/风险/待办 | ✅ | ✅ | ❌ |
| Dashboard/周报 | ✅(全部) | ✅(本人) | ✅(本人) |
| 系统管理 | ✅ | ❌ | ❌ |

---

## 二、健康评分

### 2.1 公式
```
总分 = 时间 × 35% + 风险 × 40% + 交付 × 25%
```

### 2.2 数据不足判定
条件：所有阶段 plan_end 均未到期 **且** 无待处理风险
→ 不显示分数和颜色，显示"暂无可计算数据"（灰色）

### 2.3 维度明细
每个维度返回 `score + detail`（人类可读的计算依据）：
- 时间：`"共5个阶段已到期，2个延期共12天（每天扣2分）"`
- 风险：`"高危1(-15) 中危2(-16) 低危1(-3) 停滞0(-0)"`
- 交付：`"5个阶段已到期，5个按时完成"`

### 2.4 业务状态 vs 健康状态
| 维度 | 字段 | 取值 | 来源 |
|---|---|---|---|
| 业务状态 | status | 未启动/进行中/暂停/验收中/已关闭 | 手动设置 |
| 健康状态 | healthColor | 绿/黄/红/灰 | 自动计算 |

---

## 三、进度与状态联动

| 进度 | 状态 | 额外动作 |
|---|---|---|
| 0% | 未开始 | — |
| 1%-99% | 进行中 | 自动填写实际开始日期 |
| 100% | 已完成 | 自动填写实际结束日期 |

进度 < 100 时清除 actual_end（允许回退进度）。

---

## 四、API 接口清单

统一前缀：`/api/v1`，返回：`{code, message, data}`

### 4.1 认证 Auth
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /auth/login | 登录 |
| POST | /auth/verify-username | 验证账号 |
| POST | /auth/reset-password | 重置密码（发邮件） |
| GET | /auth/permissions | 当前用户权限树 |

### 4.2 用户管理
| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST/PUT/DELETE | /users | 用户CRUD |
| GET | /users/me | 个人资料 |
| PUT | /users/me | 更新个人资料 |
| PUT | /users/me/password | 修改密码 |

### 4.3 角色管理
| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST/PUT/DELETE | /roles | 角色CRUD（内置角色不可删） |
| GET/PUT | /roles/{id}/permissions | 权限分配 |
| GET/PUT | /roles/{id}/data-scope | 数据权限 |

### 4.4 项目
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /projects | 列表（支持name/projectCode/level/status查询） |
| POST/PUT/DELETE | /projects | CRUD |
| PUT | /projects/{id}/wbs-url | WBS在线链接 |
| POST/GET | /projects/{id}/wbs-file | WBS附件上传/下载 |

### 4.5 阶段/风险/待办
| 模块 | 关键接口 |
|---|---|
| 阶段 | PUT /stages/{id}（进度联动状态，异步AI探测） |
| 风险 | POST/GET /projects/{id}/risks，GET /risks/aggregated |
| 待办 | GET /todos/page（全局分页），POST /todos（全局创建） |

### 4.6 AI/Dashboard/配置/邮件/变更/报告/日志/定时任务
| 模块 | 关键接口 |
|---|---|
| AI建议 | GET /ai-suggestions/page，POST /ai-suggestions/scan |
| Dashboard | GET /dashboard/summary，GET /dashboard/health |
| 配置 | GET/PUT /config，POST /config/test-ai |
| 邮件 | POST /digest/test，POST /digest/send-now |
| 报告 | GET /reports/weekly，GET /reports/weekly/history |
| 定时任务 | GET/PUT/POST /scheduled-tasks（含toggle/cron/run/logs） |
| 日志 | GET /operation-logs |

---

## 五、定时任务设计（DynamicScheduler）

### 5.1 任务清单
| 任务 | 默认Cron | 说明 |
|---|---|---|
| refreshDelayedStages | 每小时 | 阶段延期刷新 |
| refreshStaleRisks | 每小时:30 | 风险停滞刷新 |
| refreshOverdueTodos | 每小时:15 | 待办逾期+自动创建风险 |
| nightlyAiRiskScan | 每天22:00 | AI全量扫描 |
| dailyTodoAndRiskDigest | 每天9:20 | 项目待办与风险日报 |
| dailyDigest | 每天9:30 | 每日管理摘要 |

### 5.2 动态调度
- 启动时从 `scheduled_task` 表读取Cron并调度
- 修改Cron或切换启用/禁用后立即重新调度
- 执行前检查数据库中的启用状态
- 每次执行自动记录日志

### 5.3 待办日报过滤逻辑
- 进行中项目：始终纳入
- 已完成项目：运维阶段未完成则纳入，运维已完成则排除

---

## 六、文件上传设计（WBS附件）

- 存储路径：`{upload-root}/{项目编号}/{日期}/{UUID}.扩展名`
- 原始文件名存入 `wbs_offline_name` 字段
- 覆盖式替换，最大50MB
- 下载使用 fetch + Authorization header

---

## 七、操作日志设计

- AOP切面拦截 POST/PUT/DELETE，GET 不记录
- 按URL路径自动识别操作模块
- 记录：模块、操作类型、描述、操作人、请求方法/URL/参数、IP、耗时

---

## 八、验收标准

| 编号 | 验收项 | 状态 |
|---|---|---|
| AC-1 | 项目编号重复报错，创建后7阶段自动初始化 | ✅ |
| AC-2 | 阶段延期自动判定 | ✅ |
| AC-3 | 风险停滞判定+手动覆盖 | ✅ |
| AC-4 | 风险编号自动生成 | ✅ |
| AC-5 | Dashboard三区块+风险可点击跳转+评分明细 | ✅ |
| AC-6 | 健康评分权重校验+数据不足灰色显示 | ✅ |
| AC-7 | 邮件自动发送+AI降级+测试发送 | ✅ |
| AC-8 | AI异步探测+建议表+采纳创建正式风险 | ✅ |
| AC-9 | 周报生成+AI总结+历史记录 | ✅ |
| AC-10 | 定时任务动态调度+Cron可编辑+手动确认 | ✅ |
| AC-11 | 403跳登录+动态权限+数据权限过滤 | ✅ |
| AC-12 | 项目结束校验（排除运维） | ✅ |
| AC-13 | WBS文档管理（在线链接+离线附件） | ✅ |
| AC-14 | 角色管理CRUD+权限分配+内置角色保护 | ✅ |
| AC-15 | 业务状态与健康状态独立展示 | ✅ |
| AC-16 | 待办管理+逾期自动转风险 | ✅ |

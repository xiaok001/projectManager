# 多项目管理与风险提醒系统 —— 详细设计文档

版本：v1.0（配套PRD v1.0，MVP阶段）
说明：本文档是对《多项目管理系统_PRD概要设计.md》的补充，聚焦"怎么做"，供开发（含AI辅助开发）时作为可执行依据。数值类参数（权重、扣分系数等）均建议做成可配置项，以下为MVP默认值。

---

## 一、角色与权限矩阵

### 1.1 角色定义
| 角色 | 说明 |
|---|---|
| 部门经理（DEPT_MANAGER） | 超级管理员权限，可看/改所有项目，管理全局配置 |
| 项目经理（PM） | 只能操作自己负责的项目 |

### 1.2 权限矩阵

| 资源/操作 | 部门经理 | 项目经理（本人项目） | 项目经理（他人项目） |
|---|---|---|---|
| 项目 - 创建立项 | ✅ | ✅（创建后自动成为该项目PM，或由部门经理指定PM） | ❌ |
| 项目 - 编辑基础信息（等级/金额等） | ✅ | ✅ | ❌ |
| 项目 - 查看 | ✅（全部） | ✅（本人） | ❌（列表不可见） |
| 阶段 - 更新进度/备注 | ✅ | ✅ | ❌ |
| 风险 - 登记/更新/关闭 | ✅ | ✅ | ❌ |
| 客户满意度 - 填写 | ✅ | ✅ | ❌ |
| 变更记录 - 登记 | ✅ | ✅ | ❌ |
| AI风险建议 - 采纳/忽略 | ✅ | ✅（本人项目） | ❌ |
| Dashboard - 查看 | ✅（跨项目聚合） | ✅（仅本人项目的聚合视图） | - |
| 全局配置（阈值/发送时间/收件人） | ✅ | ❌ | ❌ |
| 周报/月报 - 查看 | ✅（全部） | ✅（本人项目） | ❌ |
| 邮件发送记录 - 查看 | ✅ | ❌ | ❌ |

**说明**：项目经理默认按"负责项目"过滤所有数据，后端所有查询接口需在服务层强制加上 `project_id in (当前用户负责的项目)` 的过滤条件（部门经理豁免），不能只在前端做隐藏。

---

## 二、项目健康评分 —— 计算公式（MVP默认值，全部可配置）

### 2.1 总分构成
```
健康总分 = 时间维度得分 × 35% + 风险维度得分 × 40% + 交付维度得分 × 25%
```
（权重存于 SystemConfig，key: `health_weight_time` / `health_weight_risk` / `health_weight_delivery`，三者之和应为100%，界面应做校验）

### 2.2 时间维度得分（time_score）
针对该项目所有"计划结束时间已过"的阶段：
```
time_score = 100 - Σ(每个已到期阶段的延期天数 × 2)，下限0
```
- 延期天数 = 若阶段已完成：max(0, actual_end - plan_end)；若阶段未完成：max(0, 今天 - plan_end)
- 扣分系数（每天2分）存于配置 `time_delay_penalty_per_day`

### 2.3 风险维度得分（risk_score）
```
risk_score = 100 - (高危风险数 × 15 + 中危风险数 × 8 + 低危风险数 × 3) - (停滞风险数 × 10)，下限0
```
- 仅统计状态为"待处理/处理中"的风险（已关闭/已解决不计入）
- 权重系数存于配置：`risk_penalty_high` / `risk_penalty_medium` / `risk_penalty_low` / `risk_penalty_stale`

### 2.4 交付维度得分（delivery_score）
```
delivery_score = (按时完成的阶段数 ÷ 已到应完成时间的阶段总数) × 100
```
- "按时完成"指 actual_end ≤ plan_end 的已完成阶段
- 分母为0（项目刚立项，尚无阶段到期）时，delivery_score 默认按100计

### 2.5 健康度颜色映射
| 总分区间 | 颜色 |
|---|---|
| ≥80 | 绿 |
| 60-79 | 黄 |
| <60 | 红 |
（分段值存配置：`health_score_green_min` / `health_score_yellow_min`）

### 2.6 客户满意度（不参与上述总分）
- 独立字段展示，1-10分制，月度更新
- 用途：项目经理个人考核参考，不进入项目健康度红黄绿判断

---

## 三、API 接口清单（RESTful，MVP范围）

统一前缀：`/api/v1`，返回统一结构 `{code, message, data}`

### 3.1 项目 Project
| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| POST | /projects | 创建项目（含project_code唯一性校验） | 部门经理/PM |
| GET | /projects | 项目列表（按当前用户角色自动过滤） | 部门经理/PM |
| GET | /projects/{id} | 项目详情 | 部门经理/本人PM |
| PUT | /projects/{id} | 编辑项目基础信息 | 部门经理/本人PM |
| PUT | /projects/{id}/satisfaction | 更新客户满意度（月度） | 部门经理/本人PM |

### 3.2 阶段 ProjectStage
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /projects/{id}/stages | 获取该项目全部阶段 |
| PUT | /stages/{id} | 更新阶段（起止时间/状态/备注），保存后触发AI隐性风险探测异步任务，并返回是否需要弹出变更登记提示 |

### 3.3 风险/问题 ProjectRisk
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /projects/{id}/risks | 登记风险（risk_code后端自动生成） |
| GET | /projects/{id}/risks | 项目风险列表 |
| GET | /risks/aggregated | 跨项目风险聚合（Dashboard用，按等级+严重程度+停滞天数排序） |
| PUT | /risks/{id} | 更新风险状态/处理措施，更新last_updated_at |
| PUT | /risks/{id}/stale-override | 手动调整停滞判定标记 |

### 3.4 Dashboard
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /dashboard/summary | 首页三区块聚合数据（风险聚合区/未来节点区/健康度总览） |

### 3.5 全局配置 SystemConfig
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /config | 获取全部配置 |
| PUT | /config | 更新配置（仅部门经理） |

### 3.6 邮件摘要 EmailDigest
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /digest/send-now | 手动触发一次摘要发送（测试用） |
| GET | /digest/logs | 邮件发送记录查询（分页） |

### 3.7 变更记录 ProjectChangeLog
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /projects/{id}/changes | 登记一条变更记录 |
| GET | /projects/{id}/changes | 该项目变更历史（复盘用） |

### 3.8 AI风险建议 AiRiskSuggestion
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /ai-suggestions?status=pending | 待确认的AI风险建议列表 |
| POST | /ai-suggestions/{id}/accept | 采纳，自动创建正式风险记录 |
| POST | /ai-suggestions/{id}/ignore | 忽略 |

### 3.9 报告 Report
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /reports/weekly?projectId=&week= | 获取/生成周报（含AI叙述） |
| GET | /reports/monthly?projectId=&month= | 获取/生成月报（含AI叙述） |

---

## 四、AI服务抽象接口设计

```
interface AiProvider {
    String chat(String prompt);  // 同步调用，返回模型生成文本
}

class DeepSeekProvider implements AiProvider { ... }   // 默认实现，调用DeepSeek云端API
class OllamaProvider implements AiProvider { ... }      // 本地部署实现，调用本地Ollama服务
```
- 通过配置 `ai_provider = deepseek | ollama` 切换，默认 `deepseek`
- 三个AI功能（每日摘要生成、隐性风险探测、周报月报叙述生成）统一通过 `AiProvider` 调用，业务代码不感知具体是哪个模型
- 调用失败需有重试和降级逻辑：调用失败时，摘要/报告退化为纯结构化文本（不含AI总结），并记录失败日志，不能因AI调用失败导致整个功能不可用

---

## 五、验收标准（Acceptance Criteria）

### AC-1 项目立项
- [ ] 项目编号重复时，提交应返回明确错误提示，不允许保存
- [ ] 项目创建后，7个标准阶段自动初始化（状态为"未开始"）

### AC-2 阶段延期自动判定
- [ ] 某阶段plan_end已过、actual_end为空时，该阶段状态自动显示"已延期"，无需人工标记
- [ ] 阶段一旦填写actual_end，延期判定立即停止变化（不再因"今天"变化而改变已完成阶段的状态）

### AC-3 风险停滞判定
- [ ] 风险最近更新时间超过配置阈值（默认7天）天数未变化，自动标红显示"停滞"
- [ ] 部门经理/PM可手动设置 `stale_override`，覆盖自动判定结果，且覆盖后不再被系统自动改回

### AC-4 风险编号生成
- [ ] 新建风险时risk_code由后端自动生成，前端不可编辑
- [ ] 同一项目同一天新建多条风险时，编号不重复（自动追加序号）

### AC-5 Dashboard
- [ ] 首页三区块（风险聚合/未来节点/健康度总览）在3-5个项目量级下一屏内可完整浏览，无需分页
- [ ] 风险聚合区排序结果符合"项目等级→严重程度→停滞天数"的规则

### AC-6 健康评分
- [ ] 三项自动维度权重之和必须为100%，配置页保存时校验，否则拒绝保存
- [ ] 客户满意度分数不影响健康总分数值（可用测试用例验证：改动满意度分数，总分不变）

### AC-7 每日邮件摘要
- [ ] 到达配置的发送时间点，系统自动发送邮件，且无论成功失败都在EmailDigestLog留一条记录
- [ ] AI调用失败时，邮件仍正常发出，内容降级为结构化列表（不含AI总结段落）

### AC-8 AI隐性风险探测
- [ ] 阶段备注保存后，异步触发探测，不阻塞保存操作的响应时间
- [ ] 探测结果仅写入AiRiskSuggestion表（状态"待确认"），不会自动写入正式ProjectRisk表
- [ ] 采纳操作后，自动在ProjectRisk表创建一条新记录，且risk_code按规则生成

### AC-9 变更记录联动提醒
- [ ] 阶段更新保存成功后，前端弹出"是否登记变更"提示，用户可选择跳转填写或忽略，忽略不影响阶段更新本身已保存的结果

### AC-10 权限隔离
- [ ] 项目经理调用任意项目相关接口时，传入非本人负责的project_id应返回403，而不是返回空数据（避免通过接口探测出其他项目是否存在）

---

## 六、待补充说明

以上公式的具体权重/扣分系数为建议默认值，属于业务判断，建议试运行1-2个真实项目周期后，根据实际感受到的评分是否符合直觉，再做微调——这类参数不建议在MVP阶段就追求"一步到位"，先跑起来，再校准。
